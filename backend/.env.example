PORT=8787
HOST=0.0.0.0
MAX_FILE_MB=100

# Optional: force a specific ffmpeg binary path.
# FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe

# Optional: CORS allowed origins (comma-separated).
# CORS_ORIGIN=http://localhost:5173,https://the-ctrl-j.github.io

# Optional encoder hooks per Nintendo format.
# IMPORTANT: these commands are required for Nintendo output generation.
# If a command is missing for requested format, API returns HTTP 501.
# Placeholders available:
# {input} {output} {format} {loopEnabled} {loopStart} {loopEnd}
# {loopVgaudioArgs} (auto: "-l start-end" or "--no-loop")
# {loopNintendoWaveArgs} (auto: "--loopStart N --loopEnd M" or empty)
#
# Example:
# NINCONVERT_BRSTM_CMD="C:/ninconvert-tools/VGAudioCli.exe -c -i {input} -o {output} {loopVgaudioArgs}"
# NINCONVERT_BCSTM_CMD=""
# NINCONVERT_BFSTM_CMD=""
# NINCONVERT_BWAV_CMD=""
# NINCONVERT_BCWAV_CMD="C:/ninconvert-tools/ctr_WaveConverter32/ctr_WaveConverter32.exe --dspadpcm {loopNintendoWaveArgs} -o {output} {input}"
# NINCONVERT_BFWAV_CMD="C:/ninconvert-tools/SoundMaker/NW4F_WaveConverter.exe --dspadpcm {loopNintendoWaveArgs} -o {output} {input}"
